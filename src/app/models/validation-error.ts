export enum ValidationError {
  REQUIRED = 'required',
  IS_NAME_UNIQUE = 'isNameUnique',
  EMAIL = 'email',
  MIN = 'min',
  INVALID_BUILDING_NUMBER = 'invalidBuildingNumber',
  INVALID_POSTAL_CODE = 'invalidPostalCode',
}
